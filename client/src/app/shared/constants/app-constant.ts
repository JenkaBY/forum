const clientId = 'forum-web';
const secret = 'secret';

export class AppConstant {
  public static get getClientId(): string {
    return clientId;
  }

  public static get getSecret(): string {
    return secret;
  }
}
